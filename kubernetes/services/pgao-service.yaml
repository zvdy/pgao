apiVersion: v1
kind: Service
metadata:
  name: pgao-service
  labels:
    app: pgao
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: pgao